{
    "name": "server",
    "dockerComposeFile": ["../../docker-compose.yaml"],
    "service": "server",
    "shutdownAction": "stopCompose",
    "workspaceFolder": "/usr/src/app/server",
    "remoteUser": "nonroot",
    "customizations": {
        "vscode": {
            "extensions": [
                "ms-python.python"
            ]
        }
    }
}